workflows:
  ios-debug:
    name: iOS Debug Build
    environment:
      flutter: stable
      xcode: latest
    scripts:
      - name: Clean project
        script: |
          flutter clean
      - name: Install dependencies
        script: |
          flutter pub get
      - name: Update CocoaPods & resolve conflicts
        script: |
          cd ios
          pod repo update
          pod install
          pod update GTMSessionFetcher/Core
          cd ..
      - name: Build iOS debug
        script: |
          flutter build ios --debug --no-codesign
    artifacts:
      - build/ios/iphoneos/*.app
      - build/ios/**/*.dSYM
