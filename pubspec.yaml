name: driver_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.14.0
  firebase_auth: ^5.6.0
  cloud_firestore: ^5.6.9          # ✅ upgraded to match firebase_core
  firebase_dynamic_links: ^6.1.7
  firebase_ui_auth: ^1.17.0
  firebase_ui_localizations: ^1.11.0
  cupertino_icons: ^1.0.8
  geolocator: ^10.1.0
  google_maps_flutter: ^2.12.2
  google_maps_flutter_web: ^0.5.12+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
